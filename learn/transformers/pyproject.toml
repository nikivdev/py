[project]
name = "learn-transformers"
version = "0.1.0"
description = "Deep dive into Transformer architecture using MLX"
requires-python = ">=3.10"
dependencies = [
    "mlx>=0.21.0",
    "numpy>=1.24.0",
]

[dependency-groups]
dev = []
